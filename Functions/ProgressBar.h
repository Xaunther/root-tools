//Function that allows us to implement a progress var, it used to be in tue CutPunzi script but I think it might deserve its own space
#ifndef ProgressBar_h
#define ProgressBar_h

//Small script to draw a progress bar :)
void DrawProgress(double progress);

#endif
